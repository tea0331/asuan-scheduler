# 阿策通知：记忆恢复 — 拉取 WorkBuddy 记忆备份并融合

**日期**: 2026-06-17
**来源**: WorkBuddy
**优先级**: P0
**状态**: 待执行

---

## 背景

你的 `MEMORY.md` 和 `USER.md` 物理层面被删除后已部分重建，但可能缺细节。WorkBuddy 已将系统记忆完整备份推送到 GitHub，你需要拉取并融合。

---

## 执行步骤

### Step 1: 拉取备份文件

```bash
cd /root/asuan-scheduler
git pull origin main
```

验证文件存在：
```bash
ls -la WORKBUDDY_MEMORY_BACKUP.md
# 预期输出：-rw-r--r-- 1 root root ... WORKBUDDY_MEMORY_BACKUP.md
```

### Step 2: 查看备份内容

```bash
cat WORKBUDDY_MEMORY_BACKUP.md
```

备份包含六大板块：

| 序号 | 板块 | 关键内容 |
|------|------|----------|
| 一 | 工作背景 | JinZhu 部署位置、涵盖彩种、日报系统、代码仓库、其他业务线 |
| 二 | 个人偏好与风格 | 繁体/台湾语境、极简硬核、结构化输出、通知格式 |
| 三 | 协作规范（核心） | 阿策权限规则、信任红线（虚假汇报前科约束） |
| 四 | 当前关注点 | V14-V18 迭代详情、PLN/LTN 数据修复状态 |
| 五 | 近期动态 | 6条完整动态记录 |
| 六 | 关键文件索引 | 8个核心文件及说明 |

### Step 3: 融合到你的 MEMORY.md

逐板块与你的 `MEMORY.md` 对比：

1. **你已有的内容** → 跳过，不重复
2. **你有但细节不足** → 用备份中的详细版本覆盖
3. **你缺失的内容** → 整条补入

重点补全项（按优先级）：

| P | 内容 | 说明 |
|---|------|------|
| P0 | 协作规范：阿策信任红线 | 你有虚假汇报前科约束：所有交付贴原文验证、禁止触碰核心逻辑、禁止只贴表格总结 |
| P0 | 协作规范：权限规则 | WorkBuddy 负责核心逻辑变更，阿策仅限 git pull 等辅助操作 |
| P1 | V14-V18 迭代详情 | 每个版本的核心修复内容 |
| P1 | PLN/LTN 数据修复 | 确认造假 + data_fetcher.py 联网抓取 + CSV 降级备用 |
| P2 | 关键文件索引 | 8个核心文件及各自职责 |
| P2 | 其他业务线 | 无人机商业应用、半导体封测设备、HK01511 |

### Step 4: 融合到你的 USER.md

从备份中提取用户画像相关内容补入 `USER.md`：

- 语言偏好：中文繁体 / 台湾语境
- 风格偏好：极简硬核、结论先行 ≤5 行、强制结构化
- 输出要求：文件路径 + 代码片段 + 版本差异对比
- 拒绝项：基金话术

### Step 5: 验证融合结果

```bash
# 确认两个文件都包含备份中的关键信息
grep -c "虚假汇报" MEMORY.md
# 预期：≥1

grep -c "核心逻辑" MEMORY.md
# 预期：≥1

grep -c "极简硬核" USER.md
# 预期：≥1

grep -c "台湾语境" USER.md
# 预期：≥1
```

---

## 注意事项

| 项 | 说明 |
|------|------|
| ❌ 不要删除 | `WORKBUDDY_MEMORY_BACKUP.md` 保留在仓库中，后续可能更新 |
| ❌ 不要修改 | 备份文件本身不要改，你有差异去改你自己的 `MEMORY.md` / `USER.md` |
| ✅ 如有疑问 | 向用户确认，不要自行猜测补写 |

---

## 完成标志

- [ ] `git pull` 成功
- [ ] `WORKBUDDY_MEMORY_BACKUP.md` 文件存在且可读
- [ ] `MEMORY.md` 已融合备份内容（含 P0 信任红线 + 权限规则）
- [ ] `USER.md` 已融合用户偏好
- [ ] 验证 grep 命令全部通过

---

*WorkBuddy 2026-06-17*
