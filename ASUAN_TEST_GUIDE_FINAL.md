# 阿算测试指南 - 刘海蟾点金日报系统

## 📋 测试前准备（必须做）

### 1. 添加SSH key到GitHub
1. 登录GitHub → 进入 `tea0331/asuan-scheduler` 仓库
2. Settings → Deploy keys → Add deploy key
3. Title: `asuan-server-ed25519`
4. Key粘贴：
```
ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIBK4WGcZuopbN9++hXiHbwRMkQ7HeUtH8NcMU+fPmwrz asuan-scheduler@github
```
5. ✅ 勾选 `Allow write access`
6. 点 `Add key`
7. **添加完成后微信告诉阿策**，他会立即push

### 2. 拉取最新代码（SSH key添加后）
```bash
cd /path/to/asuan-scheduler
git pull origin main
```

---

## 🧪 快速测试（5分钟，必须全部通过）

### 测试1：生成今日日报
```bash
cd /path/to/asuan-scheduler
python3 generate_full_daily.py 2>&1 | tail -30
```
**✅ 预期结果**：
- 无报错信息
- 生成文件：`output/2026-05-22.md`（今天的日期）

### 测试2：检查三个彩种都有今日推荐
```bash
grep -E "今日推荐\(|刘海蟾推荐\(" output/$(date +%Y-%m-%d).md
```
**✅ 预期输出**：
```
**今日推荐(4注)**:          ← 双色球（已去重）
刘海蟾推荐(5注):       ← 双色球回测（昨天生成的）
**今日推荐(4注)**:          ← 大乐透
**今日推荐(4注)**:          ← 七星彩
```

### 测试3：检查回测按日历显示（今天05-22周五）
```bash
grep -A5 "昨日.*开奖回测" output/$(date +%Y-%m-%d).md
```
**✅ 预期结果**：
- ✅ 只显示**双色球**回测（昨天05-21周四开双色球）
- ❌ **不显示**大乐透回测（昨天没开）
- ❌ **不显示**七星彩回测（昨天没开）

---

## 🔍 详细验证（15分钟，验证修复点）

### 验证1：所有彩种每天生成推荐（不管是否开奖）
**检查方法**：
```bash
# 手动改日期测试（可选）
sudo date -s "2026-05-23 07:00:00"
cd /path/to/asuan-scheduler
python3 generate_full_daily.py 2>&1 | grep "今日推荐"
sudo date -s "$(date)"  # 恢复正确日期
```
**✅ 预期**：无论星期几，三个彩种都生成"今日推荐(4注)"

### 验证2：回测按开奖日历显示
**检查逻辑**：`generate_full_daily.py` 第180-250行

| 星期 | 昨天开奖 | 应显示回测 |
|------|---------|------------|
| 周一 | 周日(双色球) | 只有双色球 |
| 周二 | 周一(大乐透) | 只有大乐透 |
| 周三 | 周二(双色球+七星彩) | 双色球+七星彩 |
| 周四 | 周三(大乐透) | 只有大乐透 |
| 周五 | 周四(双色球) | 只有双色球 |
| 周六 | 周五(双色球+七星彩) | 双色球+七星彩 |
| 周日 | 周六(大乐透) | 只有大乐透 |

### 验证3：七星彩推荐去重
**检查方法**：查看 `generate_full_daily.py` 第240-255行
```bash
sed -n '240,255p' generate_full_daily.py
```
**✅ 预期**：存在去重逻辑（如果推荐和最新开奖>=6位相同，微调1-2位）

### 验证4：大乐透推荐修复
**检查方法**：查看 `generate_full_daily.py` 第175-215行
```bash
sed -n '175,215p' generate_full_daily.py
```
**✅ 预期**：直接实现推荐生成，不调用 `la.analyze_dlt()`

### 验证5：双色球推荐修复（已去重）
**检查方法**：查看 `generate_full_daily.py` 第115-170行
```bash
sed -n '115,170p' generate_full_daily.py
```
**✅ 预期**：直接实现推荐生成，变量名一致（`core_reds`/`ext1_reds`/`ext2_reds`/`cold_reds`）

---

## ✅ 测试检查清单

### 基础功能（必须全部✅）
- [ ] 生成今日日报无报错
- [ ] 三个彩种都有"今日推荐(4注)"
- [ ] 回测只显示昨天实际开奖的彩种
- [ ] 邮件发送成功（检查163邮箱）

### 修复验证（关键项必须✅）
- [ ] 七星彩推荐去重逻辑存在
- [ ] 大乐透推荐直接实现（不依赖类方法）
- [ ] 双色球推荐直接实现（已去重，无重复数字）

### 已知问题（已修复）
- [x] ~~双色球扩展1/2偶尔出现重复数字~~ → **已修复（10:35完成）**

---

## 📮 测试完成标志

✅ **快速测试**全部通过 → 系统基本可用，可上线  
✅ **详细验证**关键项通过 → 修复成功，100%完成  
✅ **邮件发送**成功 → 自动化流程正常  

---

## 📞 联系

有问题直接微信：
- @刘老板
- @阿策

**测试完成后**，微信告诉我结果：
- ✅ 全部通过 → 我立即push代码
- ⚠️ 有问题 → 我远程协助解决

---
生成时间：2026-05-22 10:40  
测试人：阿算  
状态：等待SSH key添加后pull代码测试
