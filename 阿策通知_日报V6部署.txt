阿策 — 日报V6部署执行指令

1. 登录服务器: ssh ubuntu@101.43.44.171
2. 拉取代码: cd /root/asuan-scheduler && git pull origin main
3. 验证文件:
   python3 -m py_compile generate_full_daily.py && echo OK
   python3 -m py_compile scheduler_simple.py && echo OK
   python3 -m py_compile daily_report_guard.py && echo OK
4. 备份旧文件:
   cp generate_full_daily.py generate_full_daily.py.bak.v5
5. 验证日报生成:
   python3 generate_full_daily.py
6. 检查输出:
   ls -la output/$(date +%Y-%m-%d).md
7. 运行质量守护:
   python3 daily_report_guard.py output/$(date +%Y-%m-%d).md

⚠️ 铁律第10条: 日报架构代码禁止修改
⚠️ generate_full_daily.py / scheduler_simple.py / daily_report_guard.py
⚠️ 这3个文件只有WorkBuddy能改，你不准动
