# 阿算测试指南 - 刘海蟾点金日报系统

## 测试目标
验证系统修复是否成功：
1. ✅ 所有彩种每天生成推荐（不管是否开奖）
2. ✅ 回测按开奖日历显示（只显示昨天实际开奖）
3. ✅ 七星彩推荐去重（避免和开奖高度相似）
4. ✅ 大乐透推荐修复（不依赖类方法冲突）
5. ⚠️ 双色球推荐（偶现重复数字，正在修）

---

## 测试前准备

### 1. 添加SSH key到GitHub（如果还没做）
1. 登录GitHub → 进入 `tea0331/asuan-scheduler` 仓库
2. Settings → Deploy keys → Add deploy key
3. Title: `asuan-server-ed25519`
4. Key粘贴：
```
ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIBK4WGcZuopbN9++hXiHbwRMkQ7HeUtH8NcMU+fPmwrz asuan-scheduler@github
```
5. ✅ 勾选 `Allow write access`
6. 点 `Add key`
7. 添加完成后微信告诉阿策，他会立即push

### 2. 拉取最新代码（SSH key添加后）
```bash
cd /path/to/asuan-scheduler
git pull origin main
```

---

## 快速测试（5分钟）

### 测试1：生成今日日报
```bash
cd /path/to/asuan-scheduler
python3 generate_full_daily.py 2>&1 | tail -30
```

**预期结果**：
- ✅ 生成成功，无报错
- ✅ 输出文件：`output/2026-05-22.md`（今天的日期）

### 测试2：检查三个彩种都有今日推荐
```bash
grep -E "今日推荐\(|刘海蟾推荐\(" output/$(date +%Y-%m-%d).md
```

**预期结果**：
```
**今日推荐(4注)**:          ← 双色球（偶现重复数字）
刘海蟾推荐(5注):       ← 双色球回测（昨天生成的）
**今日推荐(4注)**:          ← 大乐透
**今日推荐(4注)**:          ← 七星彩
```

### 测试3：检查回测按日历显示（今天05-22周五）
```bash
grep -A5 "昨日.*开奖回测" output/$(date +%Y-%m-%d).md
```

**预期结果**：
- ✅ 只显示**双色球**回测（昨天05-21周四开双色球）
- ❌ **不显示**大乐透回测（昨天没开）
- ❌ **不显示**七星彩回测（昨天没开）

---

## 详细验证（15分钟）

### 验证1：所有彩种每天生成推荐
**检查文件**：`generate_full_daily.py` 第115-250行
- ✅ 双色球：无论星期几，都生成"今日推荐(4注)"
- ✅ 大乐透：无论星期几，都生成"今日推荐(4注)"
- ✅ 七星彩：无论星期几，都生成"今日推荐(4注)"

### 验证2：回测按开奖日历显示
**检查逻辑**：`generate_full_daily.py` 第180-250行

| 星期 | 昨天开奖 | 应显示回测 |
|------|---------|------------|
| 周一 | 周日(双色球) | 双色球 |
| 周二 | 周一(大乐透) | 大乐透 |
| 周三 | 周二(双色球+七星彩) | 双色球+七星彩 |
| 周四 | 周三(大乐透) | 大乐透 |
| 周五 | 周四(双色球) | 双色球 |
| 周六 | 周五(双色球+七星彩) | 双色球+七星彩 |
| 周日 | 周六(大乐透) | 大乐透 |

**测试方法**：手动改系统日期，重新生成日报，检查回测部分。

### 验证3：七星彩推荐去重
**检查文件**：`generate_full_daily.py` 第240-255行
- ✅ 如果推荐和最新开奖>=6位相同，自动微调1-2位
- **测试**：手动改推荐号码，让它和开奖高度相似，重新生成

### 验证4：大乐透推荐修复
**检查文件**：`generate_full_daily.py` 第175-215行
- ✅ 直接实现推荐生成逻辑，不依赖 `WeightedAnalyzer` 类
- ❌ 不应出现 `analyze_dlt() missing 1 required positional argument` 错误

### 验证5：双色球推荐（已知小bug）
**检查文件**：`generate_full_daily.py` 第115-170行
- ⚠️ 扩展1/扩展2偶尔出现重复数字（如 `[9, 9, 14, 14, 22, 22]`）
- ✅ **不影响使用**：忽略重复，按策略选号
- 🔧 **正在修**：阿策会在10:30前修完

---

## 测试检查清单

### ✅ 基础功能
- [ ] 生成今日日报无报错
- [ ] 三个彩种都有"今日推荐(4注)"
- [ ] 回测只显示昨天实际开奖的彩种
- [ ] 邮件发送成功（检查163邮箱）

### ✅ 修复验证
- [ ] 七星彩推荐去重逻辑存在
- [ ] 大乐透推荐直接实现（不依赖类方法）
- [ ] 双色球推荐直接实现（不依赖模块函数）

### ⚠️ 已知问题
- [ ] 双色球扩展1/2偶现重复数字（不影响使用）

---

## 测试完成标志

✅ **快速测试**全部通过 → 系统基本可用  
✅ **详细验证**关键项通过 → 修复成功  
⚠️ **双色球重复bug** → 阿策正在修（10:30前完成）

---

## 联系

有问题直接微信：
- @刘老板
- @阿策

**测试完成**后，微信告诉我结果，我会更新系统 🎯

---
生成时间：2026-05-22 10:30  
测试人：阿算  
状态：等待SSH key添加后pull代码测试
